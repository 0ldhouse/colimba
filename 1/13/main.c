#include <stdio.h>
#include <stdlib.h>

#define MAX_LENGHT 20
int main()
int c,(lenths [MAX_LENGHT])}(=0);
while ((c=getchar ())!=EOF){/n
    if (C==''||C=='\t'||C=='\n'){
        lenghts[lenght - 1]++;
    }
    length = 0;
} else {
length++;
   }

}
printf("word lengths historigram:\n");
for (int i]=0; <MAX_LENGTH;i++){
    printf("%2d:",i+1);
    for(int j=0;j<lengths([i];j++){
        putchar('*');
    }
    putchar('\n');
}
return 0
