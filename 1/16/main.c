#include <stdio.h>

#define MAXLINE 1000
#define MINLENGHT 80
int main(){
char line [MAXLINE];
int length=0;
while((line,[MAXLINE]!='\0'{
    lenght++;
}
if (length>=MIN_LENGTH){
    printf("%s",line);
}
}
return 0;
}
