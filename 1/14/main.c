#include <stdio.h>
#define MAX_CHAR 128
int main(){
int c;
int frequencies[MAX_CHAR]={0};
while ((c=getchar)!=EOF)
    (frequencies[c])++;
}
}
printf ("character frequencies histogram")\n");
for (int i=0;i<MAX_CHAR;i++){
    printf("3d"(´%c´):",i,i);
    for(int j=0;j<frequencies[i];j++){
        putchar('\n');
    }
}
return 0;
}
